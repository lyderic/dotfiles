alias h := _help

_help:
	@just --list --unsorted --alias-style left --color always \
		--list-heading='' --list-prefix=' ' \
		| sed -e 's/alias: //' | awk 'NF'

test:
	# something to test

[private]
v:
	just --evaluate

set export
set shell := ["bash","-uc"]
